# =============================================================================
#  Shell Environment Variables (tokens/shell.d/env.toml)
#  Note: Default applications (EDITOR, BROWSER, TERMINAL) are defined in tokens/apps.toml
# =============================================================================

[env]
SHOELACE_DIR = "~/shoelace"
SL_AGY_ALIAS = "~/agy-conv/sl-agy"
CARGO_TARGET_DIR = "/dev/shm/cargo"
