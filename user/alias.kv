# =============================================================================
#  Shell Aliases & Abbreviations (alias.kv)
# =============================================================================

# -----------------------------------------------------------------------------
# 📁 Directory Navigation Shortcuts
# -----------------------------------------------------------------------------
.nu = cd ~/.config/nushell/
.l = cd ~/.local/
.b = cd ~/.local/bin/
.s = cd ~/.local/share/
y = yazi
# -----------------------------------------------------------------------------
# 🛠️ Modern CLI Replacements & Utilities
# -----------------------------------------------------------------------------
ls = eza --icons --group-directories-first
ll = eza -l --icons --git --group-directories-first
la = eza -la --icons --git --group-directories-first
lt = eza --tree --icons --level=2
cat = bat --style=header,grid
find = fd
grep = rg
ps = procs
top = btm

# -----------------------------------------------------------------------------
# ⚡ General Utilities & Environment
# -----------------------------------------------------------------------------
dev = dev_env
q = exit
c = clear

# -----------------------------------------------------------------------------
# ❄️ Nix & Home Manager
# -----------------------------------------------------------------------------
d = dot
hms = home-manager switch --flake . || nix run .#homeConfigurations.(whoami).activationPackage

# -----------------------------------------------------------------------------
# 🖥️ CLI Tools & Toys
# -----------------------------------------------------------------------------
h = herdr
cmatrix = cmatrix -C blue
glow = glow -t

# -----------------------------------------------------------------------------
# 🪟 Wayland Compositors (silent launch from TTY)
# -----------------------------------------------------------------------------
n = niri >/dev/null 2>&1
l = labwc >/dev/null 2>&1
